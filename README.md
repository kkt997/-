## 项目说明

本代码运行于 **Kaggle 环境**，无需额外配置。

只需修改代码中的 `TASK_NAME` 变量，即可切换不同的数据集任务，当前支持：

- `us economy`
- `Kavanaugh`
- `german anger`
- `eu position`

## 数据文件说明

`csv/` 文件夹中 **仅包含 German Anger 和 EU Position 对应的 CSV 文件**，  
代码运行时仅依赖上述两个任务的数据文件。

## 结果说明

`results/` 文件夹中存放的是各个实验结果的**运行截图**，用于结果展示与对比。
